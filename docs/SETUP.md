# Setup

## Prerequisites
- Flutter SDK installed and available in PATH
- Android SDK configured
- An Android emulator/device for testing

## Install dependencies
```bash
flutter pub get
```

## Configure
- Ensure `android/local.properties` contains `flutter.sdk` (handled by Flutter tooling).

